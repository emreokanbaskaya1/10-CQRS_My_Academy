@model List<MyAcademyCQRS.Entities.PhotoGallery>
@{ ViewData["Title"] = "📸 Galeri Yönetimi"; }
<div class="d-flex justify-content-between align-items-center mb-4">
    <h5 class="mb-0">📸 Galeri</h5>
    <a href="@Url.Action("Create")" class="btn btn-bakery"><i class="fas fa-plus me-2"></i> Yeni Fotoğraf</a>
</div>
<div class="row">
    @foreach (var p in Model) {
    <div class="col-md-3 mb-4">
        <div class="card h-100">
            <img src="@p.ImageUrl" class="card-img-top" style="height:180px;object-fit:cover;">
            <div class="card-body p-2 text-center">
                <h6 class="mb-1">📷 @p.Title</h6>
                <small class="text-muted">🏷️ @p.CategoryTag</small><br>
                <form asp-action="Delete" asp-route-id="@p.Id" method="post" class="mt-2" onsubmit="return confirm('Emin misiniz?')">
                    <button class="btn btn-sm btn-outline-danger"><i class="fas fa-trash me-1"></i> Sil</button>
                </form>
            </div>
        </div>
    </div>
    }
</div>
