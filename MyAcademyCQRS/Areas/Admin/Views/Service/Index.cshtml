@model List<MyAcademyCQRS.Entities.Service>
@{ ViewData["Title"] = "🔧 Hizmet Yönetimi"; }
<div class="d-flex justify-content-between align-items-center mb-4">
    <h5 class="mb-0">🔧 Hizmetler</h5>
    <a href="@Url.Action("Create")" class="btn btn-bakery"><i class="fas fa-plus me-2"></i> Yeni Hizmet</a>
</div>
<div class="card"><div class="card-body table-responsive">
    <table class="table table-hover">
        <thead><tr><th>#</th><th>🖼️ İkon</th><th>📝 Başlık</th><th>💬 Açıklama</th><th>🔢 Sıra</th><th>⚙️ İşlem</th></tr></thead>
        <tbody>
            @foreach (var s in Model) {
            <tr>
                <td>@s.Id</td>
                <td>
                    @if (!string.IsNullOrEmpty(s.IconUrl))
                    {
                        <img src="@s.IconUrl" alt="@s.Title" style="width:40px;height:40px;object-fit:contain;border-radius:4px;">
                    }
                    else
                    {
                        <span class="text-muted">-</span>
                    }
                </td>
                <td>@s.Title</td>
                <td>@s.Description</td>
                <td>@s.DisplayOrder</td>
                <td>
                    <a href="@Url.Action("Update", new { id = s.Id })" class="btn btn-sm btn-bakery-outline"><i class="fas fa-edit"></i></a>
                    <form asp-action="Delete" asp-route-id="@s.Id" method="post" class="d-inline" onsubmit="return confirm('Emin misiniz?')"><button class="btn btn-sm btn-outline-danger"><i class="fas fa-trash"></i></button></form>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div></div>
