@model List<MyAcademyCQRS.Entities.Testimonial>
@{ ViewData["Title"] = "⭐ Yorum Yönetimi"; }
<div class="d-flex justify-content-between align-items-center mb-4">
    <h5 class="mb-0">⭐ Müşteri Yorumları</h5>
</div>
<div class="card"><div class="card-body table-responsive">
    <table class="table table-hover">
        <thead><tr><th>#</th><th>👤 Müşteri</th><th>💬 Yorum</th><th>⭐ Puan</th><th>📋 Durum</th><th>📅 Tarih</th><th>⚙️ İşlem</th></tr></thead>
        <tbody>
            @foreach (var t in Model) {
            <tr>
                <td>@t.Id</td>
                <td>@t.CustomerName</td>
                <td style="max-width:300px;">@t.Comment</td>
                <td>@for(int i=0;i<t.Rating;i++){<i class="fas fa-star text-warning"></i>}</td>
                <td><span class="badge @(t.IsApproved ? "bg-success" : "bg-warning text-dark")">@(t.IsApproved ? "✅ Onaylı" : "⏳ Bekliyor")</span></td>
                <td>@t.CreatedDate.ToString("dd.MM.yyyy")</td>
                <td>
                    @if (!t.IsApproved) {
                    <form asp-action="Approve" asp-route-id="@t.Id" method="post" class="d-inline"><button class="btn btn-sm btn-success"><i class="fas fa-check"></i></button></form>
                    } else {
                    <form asp-action="Reject" asp-route-id="@t.Id" method="post" class="d-inline"><button class="btn btn-sm btn-warning"><i class="fas fa-times"></i></button></form>
                    }
                    <form asp-action="Delete" asp-route-id="@t.Id" method="post" class="d-inline" onsubmit="return confirm('Emin misiniz?')"><button class="btn btn-sm btn-outline-danger"><i class="fas fa-trash"></i></button></form>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div></div>
